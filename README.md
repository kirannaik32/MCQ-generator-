The Machine Learning Enabled MCQ Generator is a web-based application that automatically generates Multiple Choice Questions (MCQs) from uploaded PDF or TXT documents using Machine Learning (ML) and Natural Language Processing (NLP).

How to Run the Project

Install required libraries:

pip install flask spacy PyPDF2

Run the application:

python app.py

Open browser and go to:

http://127.0.0.1:5000
